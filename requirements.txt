# Step 1 uses Python 3.10+ and its standard library only.
# Add and pin TimesFM-3 inference dependencies after validating them in step 2.
# Add data and dashboard dependencies in the steps that introduce them.
